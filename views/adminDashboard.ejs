<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Student Dashboard</title>
    <link rel="stylesheet" href="/css/student.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.css">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
</head>

<body>
    <div class="container text-center">
        <section class="main">
            <div class="main-top">
                <h1 style="margin-left: 0;">ADMIN DASHBOARD</h1>
                <i class="fas fa-user-cog button-shadow" onclick="logout()" style="font-size: 15px; padding-top: 20px;">Logout</i>
            </div>
            <script>
                const apiKey = '<%= apiKey %>'
                async function logout() {
                    const response = await fetch('/api/admin/logout', {
                        method: 'POST',
                        headers: {
                            'Authorization': apiKey
                        }
                    })
                    if(response.status !== 200) {
                        alert('Error logging you out')
                    } else {
                        window.location.replace('/')
                    }
                }
                function formClicked() {
                    window.location.assign('/admin/admin-report')
                }
                function feeClicked() {
                    window.location.assign('/admin/student-report')
                }
                function messCardClicked() {
                    window.location.assign('/')
                }
            </script>

            <div class="main-skills justify-content-center">
                <div class="card">
                    <i class="fa-solid fa-graduation-cap"></i>
                    <h3>Admins Details</h3>
                    <p>View other Admins</p>
                    <button onclick="formClicked()">Get Started</button>
                </div>
                <div class="card">
                    <i class="fa-solid fa-money-bills"></i>
                    <h3>Mess Details</h3>
                    <p>View Mess members</p>
                    <button onclick="feeClicked()">Get Started</button>
                </div>
                <div class="card">
                    <i class="fa-sharp fa-solid fa-address-card"></i>
                    <h3>Home</h3>
                    <p>Go back to home page</p>
                    <button onclick="messCardClicked()">Get Started</button>
                </div>
            </div>
        </section>
    </div>
</body>

</html>